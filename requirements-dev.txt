-r requirements.txt

# --- testing ---------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient transport

# --- linting / formatting --------------------------------------------------
ruff>=0.4

# --- optional: pull the real Kaggle casting dataset ------------------------
kagglehub>=0.2
